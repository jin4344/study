// 프로토타입
function Member(id, memberId, password, point) {
  this.id = id;
  this.memberid = this.memberidid;
  this.password = password;
  this.point = point;
}

const memeber1=new Member(1,"hwang","3333",4000);
const memeber1=new Member(2,"hwang","2222",5600);
